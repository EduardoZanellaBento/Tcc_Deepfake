# B4.6 — Refit final nos 30 mil completos

| | |
|---|---|
| **data-limite** | **27/09/2026** |
| **custo estimado** | meio dia |
| **pré-requisito** | B4.5 com `melhor_epoca` registrada em `cnn_definida.json` |
| **entrega** | CNN final treinada nos 30.000, com `normalizacao_cnn_30k.json` |
| **o que NÃO acontece aqui** | nenhuma escolha. Nenhum hiperparâmetro novo, nenhuma métrica de decisão, nenhum toque na validação externa |

> **Este marco é mecânico e é o mais fácil do Bloco 4 — desde que o B4.5 tenha
> deixado o número.** Se você se pegar tomando uma decisão aqui, algo do B4.5 ficou
> pendente: volte e feche lá.

---

## Contexto mínimo (ler só isto)

1. `results/metricas/cnn_definida.json` — a arquitetura vencedora e `melhor_epoca`
2. `src/models/cnn.py`, `src/models/treinar_cnn.py`
3. `data/espectrogramas/normalizacao_cnn.json` — o artefato dos 27k, para espelhar o formato
4. `docs/execucao/APENDICE_A_inventario.md` — seção 7 (P3 e P6)

---

## Por que o refit existe (a resposta de banca, em três frases)

O *early stopping* precisa de um conjunto que o modelo não veja — e esse conjunto saiu
dos 30k, deixando 27k para treinar. Sem refit, a CNN final teria sido treinada em
27.000 exemplos enquanto RF e SVM treinaram em 30.000, e a comparação passaria a
carregar um fator escondido de 3.000 amostras além do modelo. O refit **remove esse
privilégio ao contrário**: retreina a arquitetura escolhida nos 30.000 completos, pelo
número de épocas já definido, e com isso **a CNN final vê as mesmas 30 mil amostras
que RF e SVM**.

A pergunta virá assim: «**por que o refit nos 30k remove o privilégio que RF e SVM
não tiveram?**» — RF e SVM nunca precisaram separar um pedaço do treino para decidir
quando parar (o RF não tem «quando parar»; o SVM converge). A CNN precisou. O refit
paga essa dívida.

---

## Os quatro passos, em ordem

### 1. Recomputar a normalização nos 30k

**Obrigatório pela P3:** «estatísticas globais do treino, calculadas somente sobre o
treino **efetivamente disponível naquele estágio**». No refit, o treino disponível são
os 30.000.

Rode **o mesmo código do B4.3**, agora sobre todas as 30.000 linhas do
`indice_treino_30k.csv`, e grave um artefato **novo**:

```
data/espectrogramas/normalizacao_cnn_30k.json
```

com `"estagio": "30k_refit"`, `"n_exemplos": 30000` e o seu próprio `hash_lista_ids`.

**Não sobrescreva `normalizacao_cnn.json`.** Os dois artefatos têm de coexistir: o
dos 27k documenta a fase de *early stopping*, o dos 30k documenta a CNN final. A
inferência do B4.7 e do B5.1 usa **o dos 30k** — e o campo `estagio` é o que impede
alguém de pegar o errado.

> **Confira e registre a diferença.** Calcule o desvio máximo entre as duas médias
> por faixa Mel e entre os dois desvios. Deve ser pequeno (os 3k adicionais são 10%
> do conjunto, estratificados). Se for grande, algo está errado no split interno ou na
> leitura do memmap. Grave o número em `results/metricas/refit_cnn.json`:
> `delta_max_media_por_mel`, `delta_max_desvio_por_mel`.

### 2. Treinar nos 30.000, por épocas fixas

```python
# Nenhum early stopping. Nenhum conjunto de validacao. Nenhuma parada
# adaptativa. O numero de epocas vem de cnn_definida.json -> melhor_epoca, e
# e FIXO por decisao do protocolo (P6).
n_epocas = json.load(open(".../cnn_definida.json"))["melhor_epoca"]
```

- mesma arquitetura, mesmos hiperparâmetros, mesma semente 42;
- **pesos da loss recomputados** para os 30k: `w = n / (k * n_c)` com
  **3.000 bonafide / 27.000 spoof** → `w_bonafide = 30000/(2*3000) = 5,0` e
  `w_spoof = 30000/(2*27000) = 0,5556`. Razão 9:1, igual — mas **derive dos números
  reais**, não reaproveite os do B4.4 por descuido;
- registre a loss de treino por época em
  `results/metricas/curva_refit_cnn.csv` — **só a de treino**, porque não há
  conjunto de acompanhamento, e isso é de propósito.

### 3. Não tocar na validação externa

Escreva isto explicitamente no código, como comentário, e no JSON, como campo:

```python
# O refit NAO avalia nada. Nem na validacao externa (que e B4.7 e serve so
# para escolher o limiar), nem no teste (lacrado ate B5.1). Um refit que
# "so da uma olhadinha" na validacao para conferir se ficou bom transforma o
# limiar do B4.7 num limiar escolhido depois de espiar — e ai o protocolo de
# RF/SVM e o da CNN deixam de ser o mesmo.
```

Se você quiser saber se o refit «ficou bom» antes do B4.7: **a loss de treino final
comparada com a do B4.5 na mesma época** é o único indicador legítimo aqui. Deve
estar na mesma ordem de grandeza. Se estiver muito diferente, investigue — a causa
provável é normalização errada (passo 1).

### 4. Salvar e assinar

```
models/cnn_final_30k.pt            (ignorado pelo git)
results/metricas/refit_cnn.json    (versionado)
```

O `refit_cnn.json` é a assinatura do modelo final. Campos mínimos:

```json
{
  "modelo": "cnn_final_30k",
  "braco": "principal",
  "n_treino": 30000,
  "n_bonafide_treino": 3000, "n_spoof_treino": 27000,
  "arquitetura": { "...": "copia integral do bloco de cnn_definida.json" },
  "hiperparametros": { "lr": 0.001, "batch": 128, "dropout": 0.0, "otimizador": "Adam" },
  "pesos_loss": {"bonafide": 5.0, "spoof": 0.5556,
                 "formula": "n / (k * n_c), mesma logica do class_weight='balanced'"},
  "n_epocas_fixas": 37,
  "origem_n_epocas": "results/metricas/cnn_definida.json -> melhor_epoca (early stopping nos 3k internos)",
  "nota_epocas": "mesmo NUMERO DE EPOCAS da fase de early stopping; como os 30k sao 11,1% maiores que os 27k, corresponde a 11,1% mais atualizacoes de gradiente — limitacao registrada",
  "normalizacao": "data/espectrogramas/normalizacao_cnn_30k.json (estagio 30k_refit)",
  "delta_max_media_por_mel": 0.0,
  "delta_max_desvio_por_mel": 0.0,
  "loss_treino_final": 0.0,
  "tempo_treino_s": 0.0,
  "semente": 42,
  "determinismo": {"fixar_seeds_torch": true, "estrito": true,
                   "limitacao": null},
  "sem_avaliacao": "nenhum conjunto de avaliacao foi tocado neste marco: a validacao externa e B4.7 e o teste e B5.1",
  "ambiente": {"gpu": "NVIDIA GeForce RTX 5060 Ti", "compute_capability": [12, 0],
               "torch": "...", "cuda": "...", "python": "3.10.11"},
  "hash_md5_indice_treino_csv": "...",
  "hash_md5_split_interno_csv": "...",
  "hash_md5_normalizacao_30k_json": "...",
  "hash_md5_subamostra_csv": "654cb796b738512388b28e15ffb14a9d",
  "hash_md5_features_csv": "51b2f439bf6f1e10237acbc620bb92d9",
  "hash_md5_split_csv": "9143f0c7b83ec2db4aa144ed5deb3402"
}
```

Os hashes dos três artefatos congelados aparecem aqui pelo mesmo motivo que aparecem
em `rf_tuned_principal.json`: sem eles, o JSON não diz **sobre quais dados** o modelo
foi treinado.

---

## Limitação a registrar no `refit_cnn.json` — complemento da L3

> Escrita em 18/09, depois do B4.5 fechar. **É registro, não investigação:** passa no
> teste da regra de escopo com **não impede a validade do experimento principal**, não
> muda nada do que este marco executa, e custa uma entrada de JSON.

A L3 (`L3-epocas-fixas-nao-sao-passos-fixos`, em `cnn_definida.json`) já registra que
37 épocas em 30.000 são 11,1% mais atualizações de gradiente do que 37 épocas em
27.000. Falta a outra metade do mesmo fato, e ela é a parte que a banca pergunta.

**A configuração vencedora do B4.5 tem `dropout = 0.0` e `weight_decay = 0`.** Não
sobrou nenhum regularizador explícito: o único freio contra o sobreajuste, naquela
fase, foi a **parada antecipada** — medida num conjunto que o modelo não via. E o
número mostra o quanto ela estava trabalhando: na época 37 a *loss* de treino era
**0,0049** contra **0,2536** nos 3k. A rede praticamente memorizou os 27 mil.

O refit remove esse freio: treina 37 épocas fixas, sem conjunto de acompanhamento,
sobre um conjunto 11,1% maior. Isso **não é erro** — é a P6, aprovada pelo orientador,
e a época foi escolhida com dado honesto. Mas a formulação precisa tem de estar
escrita, porque a pergunta virá assim: «*vocês tiraram toda a regularização e depois
tiraram o mecanismo que estava regularizando*».

**Acrescente ao `limitacoes_registradas` do `refit_cnn.json`** (a L3, herdada do B4.5,
com o complemento — `descricao`, `alternativa_considerada`, `por_que_foi_descartada`,
`razao_de_tamanho` e `percentual_a_mais_de_atualizacoes` vêm de `cnn_definida.json`,
copiados, não redigitados):

```json
{
  "id": "L3-epocas-fixas-nao-sao-passos-fixos",
  "complemento_b46": "A configuracao vencedora do B4.5 tem dropout 0.0 e weight decay 0, entao na fase de early stopping o UNICO regularizador em operacao era a propria parada antecipada, medida nos 3k: na epoca 37 a loss de treino era 0,0049 contra 0,2536 nos 3k. O refit remove esse mecanismo — treina por numero fixo de epocas, sem conjunto de acompanhamento — e ao mesmo tempo aumenta em 11,1% o numero de atualizacoes por epoca. Os dois efeitos vao na mesma direcao: o modelo final passa um pouco mais de tempo em regime de ajuste do que o modelo cuja epoca foi selecionada.",
  "por_que_nao_invalida": "A epoca foi selecionada em dado que o modelo NAO viu, e o refit apenas a aplica — nao ha escolha nova sendo feita com dado contaminado, e a validacao externa continua intocada. O protocolo e o aprovado na P6, e o refit existe justamente para que a CNN final veja as mesmas 30 mil amostras que RF e SVM. O risco residual e de grau, nao de validade: 37 epocas podem passar um pouco do ponto otimo no conjunto maior.",
  "alternativa_considerada_para_o_complemento": "Reduzir proporcionalmente o numero de epocas (37 / 1,111 = 33,3) para igualar as atualizacoes, ou reintroduzir dropout no refit.",
  "por_que_foi_descartada": "A primeira usa um numero quebrado de epocas e contraria a letra da P6 («numero FIXO de epocas»); a segunda mudaria a arquitetura entre a selecao e o modelo final, que e exatamente o que o refit existe para NAO fazer. Ambas custariam re-executar o B4.5.",
  "teste_da_regra_de_escopo": "NAO impede a validade do experimento principal",
  "destino": "limitacao no texto (B6.1), no mesmo paragrafo da L3",
  "redacao_para_o_texto": "O refit foi executado pelo mesmo numero de epocas selecionado na fase de early stopping. Como o conjunto de refit e 11,1% maior, isso corresponde a 11,1% mais atualizacoes de gradiente; alem disso, a configuracao selecionada nao emprega dropout nem weight decay, de modo que a parada antecipada era o unico mecanismo de regularizacao em operacao naquela fase e o refit, por definicao, nao o reproduz. A alternativa — fixar o numero de atualizacoes, ou reintroduzir regularizacao explicita no refit — foi considerada e descartada por fidelidade ao protocolo aprovado, e a diferenca fica registrada como limitacao."
}
```

**O que NÃO fazer com isso:** não reabrir a busca, não mexer no dropout, não ajustar o
número de épocas, não «dar uma olhadinha» na validação externa para ver se passou do
ponto. A verificação legítima é a que o passo 4 já prevê — a *loss* de treino final do
refit comparada com a do B4.5 na época 37 (0,0049). Mesma ordem de grandeza é o
esperado; muito menor sugeriria que passou do ponto, e **mesmo assim isso vira nota,
não re-treino**, porque o número que decide sai no B4.7.

> **Atenção ao md5 da auditoria.** `cnn_definida.json` registra o md5 de `cnn.py`,
> `treinar_cnn.py` e `definir_cnn.py` como evidência do isolamento da validação. Os
> três **conferem agora**. Esta limitação entra no `refit_cnn.json` produzido pelo
> B4.6 — **não** editando `cnn_definida.json` nem os três módulos auditados, sob pena
> de invalidar aquela evidência e forçar a re-execução da grade.


---

## Armadilhas deste marco

| armadilha | consequência |
|---|---|
| reusar `normalizacao_cnn.json` (27k) no refit | viola a P3 e cria uma CNN final normalizada por estatísticas de um conjunto que não é o seu treino |
| sobrescrever `normalizacao_cnn.json` | perde a documentação da fase de *early stopping*, e a melhor época deixa de ser reproduzível |
| reaproveitar os pesos da loss dos 27k | a razão continua 9:1, então **não muda nada na prática** — mas o JSON passa a registrar números que não correspondem ao conjunto. É erro de registro, e registro é o que este trabalho tem de melhor |
| «dar uma olhadinha» na validação externa | contamina o limiar do B4.7 |
| ligar *early stopping* no refit | contradiz a P6; o refit é por nº fixo de épocas |
| mudar batch size | muda o nº de atualizações por época, e o nº de épocas herdado deixa de significar o mesmo |
| esquecer de recomputar o `n_valid` dos 3.000 novos exemplos | eles já estão no `indice_treino_30k.csv` com seu `n_frames_validos` — é só não filtrar |

---

## Critério de pronto

- [ ] `data/espectrogramas/normalizacao_cnn_30k.json` existe, com
      `estagio: "30k_refit"`, `n_exemplos: 30000` e `hash_lista_ids` próprio
- [ ] `normalizacao_cnn.json` (27k) **continua existindo, intacto**
- [ ] os dois deltas máximos entre as estatísticas 27k × 30k calculados e registrados
- [ ] CNN treinada nos 30.000 por **exatamente** `melhor_epoca` épocas, sem early
      stopping
- [ ] pesos da loss **derivados de 3.000/27.000**, registrados
- [ ] `results/metricas/refit_cnn.json` completo, com `sem_avaliacao` preenchido
- [ ] `models/cnn_final_30k.pt` salvo
- [ ] **nenhuma métrica de validação ou teste neste marco** — confirme por leitura do
      código
- [ ] curva de loss de treino do refit em CSV
- [ ] `limitacoes_registradas` no `refit_cnn.json` com a L3 **e** o
      `complemento_b46` (ver seção acima)
- [ ] md5 de `cnn.py`, `treinar_cnn.py` e `definir_cnn.py` ainda conferem com o
      registrado em `cnn_definida.json` → `isolamento_validacao`
- [ ] `.gitignore`: confirme que `normalizacao_cnn_30k.json` tem exceção
      (`data/espectrogramas/*` é ignorado)
- [ ] commit feito

## Commit

```
B4.6: refit final da CNN nos 30k completos (P6)

- normalizacao_cnn_30k.json: media e desvio por faixa Mel RECOMPUTADOS sobre
  os 30.000, so sobre frames validos, com hash da lista de IDs. O artefato dos
  27k e preservado intacto — os dois documentam estagios diferentes e o campo
  `estagio` impede a troca. Deltas maximos entre as duas estatisticas
  registrados.
- CNN final treinada nos 30.000 por N epocas FIXAS, vindas de
  cnn_definida.json -> melhor_epoca. Sem early stopping, sem conjunto de
  acompanhamento, sem nenhum toque na validacao externa (B4.7) nem no teste
  (lacrado ate B5.1).
- pesos da loss recomputados das contagens reais do refit (3.000 bonafide /
  27.000 spoof -> 5,0 x 0,5556, razao 9:1).
- com o refit, a CNN final ve as MESMAS 30 mil amostras que RF e SVM: e o que
  remove o privilegio ao contrario que o split interno 27k/3k tinha criado.
- refit_cnn.json com arquitetura, hiperparametros, epocas, origem das epocas,
  determinismo, ambiente e os hashes dos tres artefatos congelados.
- limitacoes_registradas: L3 herdada do B4.5 com complemento_b46 — a
  configuracao vencedora nao tem dropout nem weight decay, entao a parada
  antecipada era o unico regularizador em operacao, e o refit por epocas fixas
  nao o reproduz, ao mesmo tempo que aumenta em 11,1% as atualizacoes por
  epoca. Registrada como limitacao; nao reabre o B4.5.
```

## Se atrasar

Este marco custa **horas**, não dias — numa rede pequena, 37 épocas em 30k são
~16 min de GPU (o B4.5 mediu 23,6 s/época em 27k). Se 27/09 chegar sem ele, o gargalo está no B4.5 (arquitetura ou
melhor época não fechadas), e a correção é fechar o B4.5 **por julgamento
documentado** em vez de por busca, e rodar o refit no mesmo dia.

**Simplificação de último recurso, se o refit não rodar por qualquer motivo
técnico:** usar a CNN treinada nos 27k como modelo final, com esta limitação escrita
em destaque na metodologia **e** nos resultados:

> Por restrição de cronograma, a CNN final é a treinada nos 27.000 exemplos da fase
> de *early stopping*, e não o refit nos 30.000. A comparação com RF e SVM carrega,
> portanto, uma diferença de 3.000 exemplos de treino (10%) além da diferença de
> modelo. Pela curva de aprendizado do RF, 10% menos treino nessa faixa custa da
> ordem de 0,01 de f1_macro — o que torna a estimativa da CNN um **limite inferior**.

Isso é honesto e defensável. **Mas é último recurso**: o refit é barato e o
orientador foi explícito de que esta é a forma aprovada, substituindo a versão que
deixaria a CNN final treinada em apenas 27k.
